# Callbacks

The callbacks module contains wrappers for projecting LangGraph callback
payloads before they reach downstream handlers. Use these helpers when a
callback should observe the same contract-curated node boundary as kit spans.

::: graph_observability_kit.callbacks
