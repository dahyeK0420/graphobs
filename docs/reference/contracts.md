# Contracts

The contracts module contains the runtime-independent state boundary model.
These APIs do not require a graph runtime or telemetry backend.
`Contract` is the shared interface implemented by node and subgraph contracts.

::: graph_observability_kit.contracts
