# API Reference

The API reference is generated from public Python docstrings.

The package root exposes the headline adoption path. Deeper primitives live on
focused reference pages for contracts, LangGraph integration, structured
logging, and tracing.

Reference pages are generated with `mkdocstrings` from public Python docstrings.
Use the concept and guide pages for workflow decisions, and use this section for
call signatures and API behavior.
